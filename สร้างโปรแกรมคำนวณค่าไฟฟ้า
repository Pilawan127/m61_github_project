# รับจำนวนหน่วยไฟฟ้าจากผู้ใช้
# input() รับข้อมูลเป็นข้อความ และ float() แปลงให้เป็นจำนวนจริง
unit = float(input("กรอกจำนวนหน่วยไฟฟ้า: "))

# คำนวณค่าไฟฟ้า โดยคิดหน่วยละ 4.50 บาท
price = unit * 4.50

# แสดงผลลัพธ์ค่าไฟฟ้าเป็นทศนิยม 2 ตำแหน่ง
print(f"ค่าไฟฟ้า = {price:.2f} บาท")
